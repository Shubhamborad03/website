import FencingQuoteDemo from "@/components/demo/FencingQuoteDemo";

export const metadata = {
  title: "Instant Quote for Fencing · Wolf AI",
  description:
    "Watch a fencing enquiry turn into a booked job in 30 seconds. Address in, boundary measured from the survey plan, price out, site visit booked.",
};

export default function FencingDemoPage() {
  return <FencingQuoteDemo />;
}
